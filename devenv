puppet apply devenv.pp

mkdir -p ~/.virtualenvs
export WORKON_HOME=~/.virtualenvs
. /usr/local/bin/virtualenvwrapper.sh

workon toast || (mkvirtualenv toast && workon toast)
pip install --requirement=requirements.txt

if [ ! -d log ]; then
   mkdir log
   touch log/command.log
fi
