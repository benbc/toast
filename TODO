?-*- mode: org; -*-

* don't serialize commands as objects
** the serialized form includes the package, so you won't be able to refactor
* use a real database for view data
** relational, with materialized views?
** are there nosql dbs that will handle the updates?
* sensible logging
* stores pipes in /var/run
* pylons -> pyramid
* use book name as key -- when renamed, keep all old names
* initialize separate proceses in top-level wrapper
